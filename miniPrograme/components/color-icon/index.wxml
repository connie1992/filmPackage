<view class="icon-view">
  <text class="iconfont icon-{{name}}" style="font-size: {{size}}px; color:{{color}};margin-top:{{top}}px;margin-right:{{right}}px;margin-left:{{left}}px;margin-bottom:{{bottom}}px;flex-shrink:0" ></text>
  <text wx:if="{{text != ''}}">{{text}}</text>
</view>