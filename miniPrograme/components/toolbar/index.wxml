<view class="toolbar" style="height: {{toolbarHeight}}px">
  <view class="tool" wx:if="{{show}}">
    <van-icon name="arrow-left" size="20px" bind:tap="back"></van-icon>
  </view>
  <view class="title">{{title}}</view>
</view>